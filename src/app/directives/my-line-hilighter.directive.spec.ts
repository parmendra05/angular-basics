import { MyLineHilighterDirective } from './my-line-hilighter.directive';

describe('MyLineHilighterDirective', () => {
  it('should create an instance', () => {
    const directive = new MyLineHilighterDirective();
    expect(directive).toBeTruthy();
  });
});
